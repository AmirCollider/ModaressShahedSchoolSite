# وب‌سایت آفلاین مدرسه شاهد

وب‌سایت **مدرسه شاهد — دبیرستان دوره دوم منطقه ۱۲ تهران (پسرانه)**.

## اطلاعات پروژه

| مورد | مقدار |
|---|---|
| نوع وب‌سایت | آفلاین |
| مدرسه | شاهد — دبیرستان دوره دوم منطقه ۱۲ تهران، پسرانه |
| محل اجرا | Cloudflare Workers |
| محل میزبانی کدها | GitHub |
| دیتابیس | Cloudflare D1 |
| نام دیتابیس D1 | `modaressshahedschoolsite-db` |
| فضای ذخیره‌سازی | Cloudflare R2 |
| نام Bucket | `modaressshahedschoolsite-r2` |
| دامنه سفارشی R2 | `https://dl.modares12.com` |

## زیرساخت پروژه

### Cloudflare Workers

کد و منطق اصلی وب‌سایت روی **Cloudflare Workers** اجرا می‌شود.

### GitHub

مخزن اصلی کدهای پروژه روی **GitHub** قرار دارد.

### Cloudflare D1

دیتابیس پروژه از **Cloudflare D1** استفاده می‌کند.

نام دیتابیس:

```text
modaressshahedschoolsite-db
```

### Cloudflare R2

فایل‌ها و منابع ذخیره‌شده پروژه روی **Cloudflare R2** نگهداری می‌شوند.

نام Bucket:

```text
modaressshahedschoolsite-r2
```

دامنه سفارشی R2:

```text
https://dl.modares12.com
```

## لوگوی سایت

مسیر لوگوی اصلی سایت:

```text
https://dl.modares12.com/Logo/Logo.png
```

---

> این فایل اطلاعات پایه و زیرساختی وب‌سایت را مستند می‌کند و می‌تواند به‌عنوان مرجع پروژه برای توسعه و نگهداری استفاده شود.
